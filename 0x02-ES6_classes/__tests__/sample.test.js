describe('sample Test Suite', () => {
  it('should be true', () => {
    // Setup code if needed
    expect.assertions(1); // Ensures the test has assertions
    expect(true).toBe(true);
  });

  it('should equal 2', () => {
    // Setup code if needed
    expect.assertions(1); // Ensures the test has assertions
    expect(1 + 1).toBe(2);
  });
});
