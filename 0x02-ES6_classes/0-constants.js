function taskFirst() {
  const task = 'I prefer const when I can.';
  return task;
}

function taskNext() {
  const task = 'But sometimes let is okay';
  return task;
}

// Example usage
console.log(taskFirst()); // Output: 'I prefer const when I can.'
console.log(taskNext()); // Output: 'But sometimes let is okay'
